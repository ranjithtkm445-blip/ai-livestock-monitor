services:
  - type: web
    name: ai-livestock-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt && python -c "import matplotlib; matplotlib.font_manager._rebuild()"
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
